# FastAPI backend

目前已初始化 application factory、CORS、OpenAPI 與 `/api/v2/health`。股票、重建、持股與 AI narrative routes 會依 `V2/docs/09_FRONTEND_BACKEND_ARCHITECTURE.md` 逐步加入。

開發環境由 V2 共用 `.venv` 提供，API 與 AI Training 使用相同 Python dependencies 和 `mindfolio-core`。
