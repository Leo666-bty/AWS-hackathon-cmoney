# HTML Slide Deck

開啟 [`index.html`](index.html) 即可播放簡報。

操作：

- `←` / `→`：切換頁面
- `Home` / `End`：第一頁／最後一頁
- 右下角按鈕：上一頁／下一頁
- 瀏覽器列印：每個 section 會輸出為一張 16:9 頁面

這是提案 Init 版本，後續只應替換團隊資料、正式架構截圖、Live Demo QR Code 與驗證後 KPI，不要改變核心故事。
